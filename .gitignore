node_modules/
.DS_Store
Thumbs.db
*.log

# Raw screen captures: sources for tools/build-screens.py, never published
# because they still carry client identity.
assets/images/but/

# Source photos for tools/build-composers.py, not committed.
tools/_photos/

# The journal itself stays local. The page is the published version.
the-color-i-was-chasing.md
